const fetchTrendData = async (namaGi) => {
    try {
      const res = await secureFetch(`${API_URL}

const fetchAvrBrowserStructure = async (refresh = false, targetPointIndex = avrBrowser.targetPointIndex) => {
    setAvrBrowser((current) => ({ ...current, loading: true, error: '', targetPointIndex }

const fetchArBrowserStructure = async (refresh = false, targetPointIndex = arBrowser.targetPointIndex) => {
    setArBrowser((current) => ({ ...current, loading: true, error: '', targetPointIndex }